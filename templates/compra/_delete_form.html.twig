<form method="post" action="{{ path('app_compra_delete', {'id': compra.id}) }}" onsubmit="return confirm('¿Estás seguro de que querés eliminar esta compra?');" class="btn-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compra.id) }}">
    <button type="submit" class="btn-link btn-delete">Eliminar</button>
</form>
