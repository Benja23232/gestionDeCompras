<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Proyecto Diócesis{% endblock %}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        @font-face {
            font-family: 'AVIAN';
            src: url('/fuentes/AVIAN.ttf') format('truetype');
            font-weight: normal;
            font-style: normal;
        }

        html, body {
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #1e1e2f, #2c3e50);
            background-size: cover;
            background-attachment: fixed;
            background-repeat: no-repeat;
            background-position: center;
            color: white;
        }

        .navbar {
            background-color: rgba(30, 30, 30, 0.9);
            padding: 8px 20px;
        }

        .navbar nav ul {
            list-style: none;
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin: 0;
            padding: 0;
            justify-content: flex-start;
        }

        .navbar nav ul li a {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            color: white;
            text-decoration: none;
            padding: 8px 12px;
            border-radius: 4px;
            transition: background 0.3s;
        }

        .navbar nav ul li a span {
            display: inline-block;
        }

        .navbar nav ul li a:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }

        .content {
            flex: 1;
            padding: 40px;
            backdrop-filter: blur(5px);
        }

        h1, h2 {
            text-align: center;
            margin-top: 0.5em;
        }

        .cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-top: 40px;
        }

        .card {
            background-color: rgba(255,255,255,0.1);
            border: 1px solid rgba(255,255,255,0.2);
            border-radius: 10px;
            padding: 20px;
            text-align: center;
            color: white;
            text-decoration: none;
            transition: background 0.3s;
        }

        .card:hover {
            background-color: rgba(255,255,255,0.2);
        }

        .card i {
            font-size: 30px;
            margin-bottom: 10px;
        }

        footer {
            text-align: center;
            padding: 15px 20px;
            color: white;
            font-size: 0.9em;
            background-color: rgba(0, 0, 0, 0.6);
            border-top: 1px solid rgba(255,255,255,0.2);
            line-height: 1.4;
        }

        footer a {
            color: rgb(2, 180, 250);
            text-decoration: none;
        }

        .custom-table {
            width: 100%;
            border-collapse: collapse;
            background-color: rgba(255, 255, 255, 0.05);
            box-shadow: 0 0 8px rgba(0,0,0,0.2);
            border-radius: 8px;
            overflow: hidden;
            color: white;
            margin-top: 20px;
        }

        .custom-table th, .custom-table td {
            border: 1px solid rgba(250, 126, 2, 0.6);
            padding: 10px;
            text-align: left;
        }

        .custom-table th {
            background-color: rgb(250, 126, 2);
            color: white;
            font-weight: bold;
        }

        .custom-table tr:nth-child(even) {
            background-color: rgba(255, 255, 255, 0.05);
        }

        .custom-table tr:hover {
            background-color: rgba(255, 255, 255, 0.1);
            transition: background 0.3s ease;
        }

        .custom-table td a {
            color: orange;
            text-decoration: none;
            font-weight: bold;
        }

        .custom-table td a:hover {
            text-decoration: underline;
        }

        @media screen and (max-width: 600px) {
            html, body {
                font-size: 16px;
                overflow-x: hidden;
            }

            .content {
                padding: 20px;
            }

            .cards {
                grid-template-columns: 1fr;
                gap: 15px;
                margin-top: 20px;
            }

            .card {
                padding: 15px;
                font-size: 1em;
            }

            .card i {
                font-size: 24px;
                margin-bottom: 8px;
            }

            footer {
                font-size: 0.85em;
                padding: 12px 16px;
                margin-top: 1.5em;
                word-wrap: break-word;
            }

            .custom-table {
                font-size: 0.9em;
                overflow-x: auto;
                display: block;
            }

            .custom-table th, .custom-table td {
                padding: 8px;
            }

            .navbar nav ul {
                flex-direction: row;
                flex-wrap: nowrap;
                justify-content: flex-start;
                overflow-x: auto;
            }

            .navbar nav ul li a {
                font-size: 0.85em;
                padding: 4px 8px;
                gap: 4px;
                white-space: nowrap;
            }

            h1, h2 {
                font-size: 1.4em;
            }
        }
    </style>

    {% block stylesheets %}{% endblock %}
    {% block javascripts %}{% endblock %}
</head>
<body>

    <div class="navbar">
        <nav>
            <ul>
                <li><a href="{{ path('app_inicio') }}"><span>🏠 Inicio</span></a></li>
                <li><a href="{{ path('app_compra_index') }}"><span>🛒 Compras</span></a></li>
                <li><a href="{{ path('app_pago_new') }}"><span>💸 Pagos</span></a></li>
                <li><a href="{{ path('deuda_index') }}"><span>⚖️ Deudas</span></a></li>
            </ul>
        </nav>
    </div>

    <div class="content">
        {% block body %}{% endblock %}
    </div>

    {% if app.user %}
    <div class="usuario-logueado">
        <span style="color:black;">Hola {{ app.user.email }}</span>
        <form action="{{ path('app_logout') }}" method="post" style="display:inline;">
            <button type="submit">Cerrar sesión</button>
        </form>
    </div>
    {% endif %}

    <footer>
        Creado por <strong>Benjamin Arancivia</strong> —
        <a href="mailto:benjaarncivia@gmail.com">benjaarncivia@gmail.com</a>
    </footer>

</body>
</html>
